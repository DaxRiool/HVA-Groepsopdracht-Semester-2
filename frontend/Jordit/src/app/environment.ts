/**
 * dit is geen config.env file omdat dit niet werkt met Angular.
 * Angular heeft een environment.ts file nodig om de environment variabelen te kunnen gebruiken.
 */

export const environment = {
  production: false,
  API_URL: 'http://localhost:3002',
};
